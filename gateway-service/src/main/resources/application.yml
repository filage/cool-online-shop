server:
  port: 8080

spring:
  application:
    name: gateway-service
  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: auth-service
              uri: http://localhost:8081
              predicates:
                - Path=/api/auth/**
              filters:
                - RewritePath=/api/auth/?(?<segment>.*), /auth/$\{segment}
            - id: user-service
              uri: http://localhost:8082
              predicates:
                - Path=/api/users/**
              filters:
                - RewritePath=/api/users/?(?<segment>.*), /users/$\{segment}
            - id: catalog-products
              uri: http://localhost:8083
              predicates:
                - Path=/api/catalog/products/**
              filters:
                - RewritePath=/api/catalog/products/?(?<segment>.*), /products/$\{segment}
            - id: catalog-categories
              uri: http://localhost:8083
              predicates:
                - Path=/api/catalog/categories/**
              filters:
                - RewritePath=/api/catalog/categories/?(?<segment>.*), /categories/$\{segment}
            - id: cart-service
              uri: http://localhost:8084
              predicates:
                - Path=/api/cart/**
              filters:
                - RewritePath=/api/cart/?(?<segment>.*), /cart/$\{segment}
            - id: order-service
              uri: http://localhost:8085
              predicates:
                - Path=/api/orders/**
              filters:
                - RewritePath=/api/orders/?(?<segment>.*), /orders/$\{segment}
            - id: payment-service
              uri: http://localhost:8086
              predicates:
                - Path=/api/payments/**
              filters:
                - RewritePath=/api/payments/?(?<segment>.*), /payments/$\{segment}

gateway:
  jwt:
    secret: ${AUTH_JWT_SECRET:dev-only-secret-key-for-auth-service-please-change}

logging:
  level:
    org.springframework.cloud.gateway: DEBUG
